# 复制此文件为 .env，填入实际值
# .env 已在 .gitignore 中，不会提交到 git

# Tushare API Token（可选，使用 tushare 数据源时必填）
# 注册地址：https://tushare.pro/register
TUSHARE_TOKEN=your_tushare_token_here

# 数据根目录覆盖（可选，默认 ~/.cq/data）
# CQ_DATA_ROOT=/path/to/your/data

# 日志级别覆盖（可选）
# CQ_LOG_LEVEL=DEBUG
